# ReelWave Clone (static prototype)

An early static prototype of the ReelWave streaming app.

## Key files

- `index.html` — the prototype page
- `assets/` — generated media assets
- `icon.jpg` — project icon

Note: the current full ReelWave app lives in `../reelwave-clone-2/`.
