# from-root

Staged root files that already live in Systems/Production.

When a file is moved here, the root copy can be deleted later.  
Until then, both may exist: root (legacy) + Systems (official) + Trash (staging).
